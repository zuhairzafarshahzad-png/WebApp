import { Calculator, Info } from 'lucide-react'; import { Modal } from '../ui/Modal'; import { getNetwork } from '../../lib/utils'; import type { TokenConfig } from '../../types/token';
export function CostDialog({config,onClose}:{config:TokenConfig;onClose:()=>void}){const n=getNetwork(config.network);return <Modal title="Estimated Deployment Cost" onClose={onClose}><div className="mode-badge">TEST / SIMULATION MODE</div><div className="cost-graphic"><Calculator/><div><span>Simulated network estimate</span><strong>~ 0.004–0.012 {n.short}</strong></div></div><div className="dialog-table"><span>Network<b>{n.label}</b></span><span>Contract standard<b>{n.standard}</b></span><span>Live gas data<b>Not connected</b></span></div><p className="dialog-note"><Info/> This is an illustrative range, not a live quote. No wallet provider or live gas source is configured.</p><div className="dialog-actions"><button className="primary" onClick={onClose}>Got it</button></div></Modal>}
