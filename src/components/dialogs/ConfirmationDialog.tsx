import { AlertTriangle, ShieldCheck } from 'lucide-react'; import { Modal } from '../ui/Modal'; import type { TokenConfig } from '../../types/token'; import { formatSupply, getNetwork, shortAddress } from '../../lib/utils';
export function ConfirmationDialog({config,wallet,onClose,onConfirm}:{config:TokenConfig;wallet:string;onClose:()=>void;onConfirm:()=>void}){const n=getNetwork(config.network);return <Modal title="Generate Flask Token" onClose={onClose}><div className="mode-badge">TEST / SIMULATION MODE</div><div className="dialog-table"><span>Token Name<b>{config.name}</b></span><span>Symbol<b>{config.symbol}</b></span><span>Network<b>{n.label}</b></span><span>Total Supply<b>{formatSupply(config.supply)}</b></span><span>Decimals<b>{config.decimals}</b></span><span>Contract Standard<b>{n.standard}</b></span><span>Estimated Network Fee<b>Simulation — no fee</b></span><span>Wallet Address<b>{wallet?shortAddress(wallet):'Not connected'}</b></span></div><div className="warning-box"><AlertTriangle/><p><strong>Custom asset notice</strong>This creates a custom token. It is not official USDT and is not issued or endorsed by Tether.</p></div><p className="dialog-note"><ShieldCheck/> Confirming runs a local simulation only. It will not request a transaction or deploy on-chain.</p><div className="dialog-actions"><button onClick={onClose}>Cancel</button><button className="primary" onClick={onConfirm}>Confirm &amp; Simulate</button></div></Modal>}
