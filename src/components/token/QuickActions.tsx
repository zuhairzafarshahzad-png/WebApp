import { ClipboardPaste, FileText, Sparkles, Trash2 } from 'lucide-react'; import { useState } from 'react';
export function QuickActions({onAi,onTemplate,onPaste,onClear}:{onAi:()=>void;onTemplate:()=>void;onPaste:()=>Promise<void>;onClear:()=>void}){const [loading,setLoading]=useState(false);const ai=()=>{setLoading(true);setTimeout(()=>{onAi();setLoading(false)},550)};return <div className="quick-actions"><button className="ai" onClick={ai} disabled={loading}><Sparkles/>{loading?<><i className="spinner"/> Generating...</>:'Generate with AI'}</button><button className="template" onClick={onTemplate}><FileText/> Use Template</button><button className="paste" onClick={onPaste}><ClipboardPaste/> Paste from Clipboard</button><button className="clear" onClick={onClear}><Trash2/> Clear All</button></div>}
