import { Beaker, Box, LockKeyhole, Users } from 'lucide-react'; import type { TokenConfig } from '../../types/token';
const presets=[['Basic Token','Balanced defaults for a standard custom token',Box,{}],['Fixed Supply','Immutable supply with optional burning',LockKeyhole,{mintable:false,burnable:true}],['Community Token','Flexible community-oriented configuration',Users,{mintable:true,burnable:true}],['Test Token','Small supply intended for testing',Beaker,{name:'Flask Test Token',symbol:'FTST',supply:'1000000',network:'bsc'}]] as const;
export function PresetsPanel({apply}:{apply:(v:Partial<TokenConfig>)=>void}){return <div className="preset-grid">{presets.map(([title,desc,Icon,values])=><button type="button" onClick={()=>apply(values)} key={title}><Icon/><strong>{title}</strong><small>{desc}</small><span>Use preset →</span></button>)}</div>}
