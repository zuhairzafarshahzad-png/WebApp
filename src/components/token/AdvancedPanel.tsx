import type { ReactNode } from 'react'; import type { TokenConfig } from '../../types/token'; import type { UseFormRegister } from 'react-hook-form';
export function AdvancedPanel({register}:{register:UseFormRegister<TokenConfig>}){return <div className="settings-panel"><div className="switch-grid"><Switch label="Mintable" help="Allow the owner to create more tokens" field={<input type="checkbox" {...register('mintable')}/>}/><Switch label="Burnable" help="Allow holders to permanently destroy tokens" field={<input type="checkbox" {...register('burnable')}/>}/><Switch label="Pausable" help="Allow the owner to pause transfers" field={<input type="checkbox" {...register('pausable')}/>}/><Switch label="Ownership Transfer" help="Permit a future owner change" field={<input type="checkbox"/>}/></div><div className="form-grid two"><label className="simple-field">Max Supply<input placeholder="Leave empty for fixed supply" inputMode="numeric"/></label><label className="simple-field">Initial Owner<input placeholder="Connected wallet address"/></label></div><label className="simple-field">Metadata URI<input placeholder="https:// or ipfs:// metadata location"/></label></div>}
function Switch({label,help,field}:{label:string;help:string;field:ReactNode}){return <label className="switch-row"><div><strong>{label}</strong><small>{help}</small></div><span className="switch">{field}<i/></span></label>}
