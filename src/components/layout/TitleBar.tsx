import { ChevronDown, Copy, Maximize2, Menu, Minus, Moon, Unplug, X } from 'lucide-react';
import { Brand } from './Brand';
import { shortAddress } from '../../lib/utils';
export function TitleBar({wallet,connecting,onConnect,onDisconnect,onMenu}:{wallet:string;connecting:boolean;onConnect:()=>void;onDisconnect:()=>void;onMenu:()=>void}){
 return <header className="titlebar"><button className="mobile-menu icon-button" onClick={onMenu} aria-label="Open navigation"><Menu/></button><Brand/><span className="version">v2.1.0</span><span className="divider"/><span className="tagline">Fast. Secure. Unlimited.</span><div className="title-actions"><button className="icon-button" aria-label="Theme: dark"><Moon size={17}/></button><button className="top-select">Mainnet <ChevronDown size={13}/></button><div className="wallet-wrap"><button className="connect-button" onClick={onConnect}>{connecting?'Connecting...':wallet?shortAddress(wallet):'Connect Wallet'}</button>{wallet&&<div className="wallet-menu"><small>CONNECTED ADDRESS</small><code>{shortAddress(wallet)}</code><button onClick={()=>navigator.clipboard?.writeText(wallet)}><Copy size={14}/> Copy Address</button><button onClick={onDisconnect}><Unplug size={14}/> Disconnect</button></div>}</div><div className="window-controls" aria-label="Decorative window controls"><span><Minus/></span><span><Maximize2/></span><span className="close"><X/></span></div></div></header>
}
