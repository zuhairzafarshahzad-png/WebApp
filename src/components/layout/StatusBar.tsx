import { Circle, Github, MessageCircle, Send } from 'lucide-react'; import { Brand } from './Brand';
export function StatusBar({connected,network}:{connected:boolean;network:string}){return <footer className="statusbar"><div><Brand compact/><strong>CryptoFlask</strong><span className="divider"/><span className="status-slogan">The Custom Token Generator for Everyone.</span></div><div><span className={connected?'online':'offline'}><Circle size={8} fill="currentColor"/> {connected?'Connected':'Wallet Not Connected'}</span><span className="status-network">Network: <b>{network}</b></span><span>Gas: --</span><span className="status-links">Docs　Support　<Github/> <MessageCircle/> <Send/></span></div></footer>}
